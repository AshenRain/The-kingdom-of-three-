import javax.swing.*;

public class MathModule {

    /// PLAYER COMMAND

    void PlayerMovement()
    {
        // игрок выбирает юнита, который не ходил
        // после выбора запускается меню выбора дейстивий фигуры (находится в классе character)
    }


    void PlayerKingdomSkill(int skill) // скил при обороне замка
    {
        // на стадии концепции
    }

    void PlayerCallUnit(int unit) // выбор юнита для призыва
    {
        // покупка юнита за ресурсы
    }

    void PlayerEndMove()
    {
        // перед передачей проверяется, остались ли фигуры, которые не походили
    }

    /// CHARACTER COMMAND

    void CharacterMove(int field_code) // передается код ячейки куда хочет перейти фигура
    {
        // проверка не занята ли ячейка
        // проверка соседняя ли это ячейка (3 формулы из теор. базиса.)
        // передвижение в нужную ячейку
    }

    void CharacterAttack(int field_code) // передается код ячейки куда хочет напасть фигура
    {
        // проверка есть ли кто-то в ячейке, и враг ли это
        // просчет получения урона
        // удаление юнитов, у которых закончились жизни
    }

    /// FIELD COMMAND

    // у поля нет комманд ;)
}
